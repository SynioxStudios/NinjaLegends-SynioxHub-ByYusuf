local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

local Window = Fluent:CreateWindow({
    Title = "Syniox Hub",
    SubTitle = "Ninja Legends"
})

local FarmTab = Window:AddTab({ Title = "Auto Farm" })
local ShopTab = Window:AddTab({ Title = "Shop" })

FarmTab:AddToggle("Auto Swing", function(state)
    _G.swing = state
    while _G.swing do
        task.wait()
        game:GetService("Players").LocalPlayer.ninjaEvent:FireServer("swingKatana")
    end
end)

FarmTab:AddToggle("Auto Sell", function(state)
    _G.sell = state
    while _G.sell do
        task.wait()
        -- Buraya sell kodunu ekle
    end
end)

ShopTab:AddButton("Unlock All Islands", function()
    print("Islands Unlocked!")
    -- Buraya ada açma kodunu ekle
end)
